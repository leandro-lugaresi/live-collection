{
  "name": "@pi-demo/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "pi-demo reference backend: Effect HttpApi (CRUD + catchup) + raw SSE route over an in-memory sync event log and Effect PubSub bus.",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@effect/platform-node": "catalog:",
    "@pi-demo/shared": "workspace:*",
    "@leandro-lugaresi/live-collection-protocol": "workspace:*",
    "effect": "catalog:",
    "@leandro-lugaresi/live-collection-server": "workspace:*"
  },
  "devDependencies": {
    "@effect/vitest": "catalog:",
    "@leandro-lugaresi/live-collection": "workspace:*",
    "@types/node": "catalog:",
    "tsx": "^4.23.1",
    "vitest": "catalog:"
  }
}
