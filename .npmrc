link-workspace-packages=true
save-workspace-protocol=rolling

# Resolve the forked scope from npm.pkg.github.com; the GITHUB_TOKEN or a PAT
# with `packages:write` is used at install/publish time. Do not commit a token.
@leandro-lugaresi:registry=https://npm.pkg.github.com